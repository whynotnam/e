#include <stdio.h>

int main() {
    
    int N;                  // N = 사람의 수
    scanf("%d", &N);    // N 입력
    
    int i;
    for ( i = 0; i < N; i++) {
    scanf("%d", &i);
   }

   int P;
    for ( i = 0; i < N; i++ ) {
	P = P + i
  }
  
 print("%d", P);
}